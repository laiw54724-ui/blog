---
import BaseLayout from '../layouts/BaseLayout.astro'
import { getArticles, getPopularTags, getProfile } from '../lib/data'
import { buildEntryCardModels, splitTagSummaryGroups } from '../lib/presenters'
import { applyPageCacheHeaders } from '../lib/response'
import { SERIES_DEFINITIONS } from '../lib/series'

const [articles, popularTags, profile] = await Promise.all([
  getArticles(),
  getPopularTags({ type: 'article', limit: 16 }),
  getProfile(),
])

const articleModels = await buildEntryCardModels(articles, { excerptLength: 150 })
const latestArticles = articleModels.slice(0, 7)
const groupedPopularTags = splitTagSummaryGroups(popularTags)
applyPageCacheHeaders(Astro.response, 'articles')
---

<BaseLayout title="文章 | Double River" description="整理過、值得回頭找的長文與評論。">
  <div class="articles-shell">
    <header class="articles-header">
      <p class="articles-kicker">category index</p>
      <h1>文章</h1>
      <p class="articles-desc">整理過、值得回頭翻找的長文、心得與評論。</p>
      <div class="articles-actions">
        <a href="/search?type=article" class="articles-search-link">搜尋文章</a>
      </div>
    </header>

    <section class="series-grid" aria-label="專題入口">
      {SERIES_DEFINITIONS.map((series) => (
        <a href={`/series/${series.slug}`} class="series-card">
          <span class="series-card-kicker">{series.kicker}</span>
          <h2>{series.title}</h2>
          <p>{series.description}</p>
        </a>
      ))}
    </section>

    <div class="articles-layout">
      <section class="articles-main" aria-label="文章清單">
        {articleModels.length > 0 ? (
          articleModels.map((vm) => (
            <article class="article-index-item">
              <a href={vm.href} class="article-index-link">
                <div class="article-index-meta">
                  <time datetime={vm.rawDate}>{vm.displayDate}</time>
                </div>
                <h2>{vm.displayTitle}</h2>
                <p class="article-index-excerpt">{vm.displayExcerpt}</p>
              </a>

              <div class="article-index-footer">
                <span>留言 {vm.metrics.comment_count}</span>
                <span>拍手 {vm.metrics.clap_count}</span>
                <span>閱讀 {vm.metrics.view_count}</span>
              </div>
            </article>
          ))
        ) : (
          <p class="empty-state">還沒有任何文章</p>
        )}
      </section>

      <aside class="articles-sidebar" aria-label="文章側欄">
        <section class="sidebar-card profile-card">
          <p class="sidebar-eyebrow">about</p>
          <h2>{profile.name}</h2>
          <p>{profile.bio || '把讀過、想過、走過的事整理成可回頭翻找的文字。'}</p>
        </section>

        <section class="sidebar-card">
          <div class="sidebar-title">最新文章</div>
          <ul class="sidebar-list">
            {latestArticles.map((article) => (
              <li>
                <a href={article.href}>{article.displayTitle}</a>
                <time datetime={article.rawDate}>{article.displayDate}</time>
              </li>
            ))}
          </ul>
        </section>

        {popularTags.length > 0 && (
          <section class="sidebar-card">
            <div class="sidebar-title sidebar-title-with-link">
              <span>熱門標籤</span>
              <a href="/tags">全部標籤</a>
            </div>
            <div class="sidebar-tag-groups">
              {groupedPopularTags.structured.length > 0 && (
                <section class="sidebar-tag-section">
                  <p class="sidebar-tag-section-kicker">structured tags</p>
                  {groupedPopularTags.structured.map((group) => (
                    <section class="sidebar-tag-group">
                      <h3>{group.label}</h3>
                      <div class="sidebar-tag-cloud">
                        {group.tags.map((tag) => (
                          <a href={`/tags/${tag.slug}`} class="sidebar-tag">
                            <span>#{tag.normalizedLabel}</span>
                            <span>{tag.entry_count}</span>
                          </a>
                        ))}
                      </div>
                    </section>
                  ))}
                </section>
              )}
              {groupedPopularTags.free.length > 0 && (
                <section class="sidebar-tag-section">
                  <p class="sidebar-tag-section-kicker">free tags</p>
                  {groupedPopularTags.free.map((group) => (
                    <section class="sidebar-tag-group">
                      <h3>{group.label}</h3>
                      <div class="sidebar-tag-cloud">
                        {group.tags.map((tag) => (
                          <a href={`/tags/${tag.slug}`} class="sidebar-tag sidebar-tag-free">
                            <span>#{tag.normalizedLabel}</span>
                            <span>{tag.entry_count}</span>
                          </a>
                        ))}
                      </div>
                    </section>
                  ))}
                </section>
              )}
            </div>
          </section>
        )}
      </aside>
    </div>
  </div>
</BaseLayout>

<style>
  .articles-shell {
    max-width: 1080px;
    margin: 0 auto;
    padding: 2.8rem 1.5rem 3rem;
  }

  .articles-header {
    margin-bottom: 2.75rem;
    padding-bottom: 1.35rem;
    border-bottom: 1px solid var(--border);
  }

  .articles-kicker {
    margin: 0 0 0.45rem;
    font-size: 0.8rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--text-muted);
  }

  .articles-header h1 {
    font-family: var(--serif);
    font-size: clamp(2.9rem, 6vw, 3.35rem);
    font-weight: 700;
    line-height: 0.98;
    letter-spacing: -0.035em;
    margin-bottom: 0.65rem;
    color: #121218;
  }

  .articles-desc {
    max-width: 36rem;
    color: #6f6a86;
    font-size: 1rem;
    line-height: 1.75;
  }

  .articles-actions {
    margin-top: 1rem;
  }

  .articles-search-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.72rem 1.35rem;
    border: 1px solid var(--border);
    border-radius: 999px;
    color: var(--accent);
    background: transparent;
    font-size: 0.92rem;
    font-weight: 600;
  }

  .articles-search-link:hover {
    background: var(--accent-bg);
    border-color: #cbc5ee;
  }

  .articles-layout {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 280px;
    gap: 2.5rem;
    align-items: start;
  }

  .series-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 0.85rem;
    margin-bottom: 1.8rem;
  }

  .series-card {
    padding: 1.25rem 1.2rem 1.2rem;
    border: 1px solid var(--border);
    border-radius: 22px;
    background: transparent;
    box-shadow: none;
  }

  .series-card-kicker {
    display: block;
    margin-bottom: 0.85rem;
    color: var(--text-muted);
    font-size: 0.78rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    text-decoration: underline;
    text-underline-offset: 0.18em;
  }

  .series-card h2 {
    margin: 0 0 0.6rem;
    font-size: 1.1rem;
    color: #28089e;
    font-family: var(--serif);
  }

  .series-card p {
    margin: 0;
    color: #58546e;
    line-height: 1.72;
    font-size: 0.9rem;
  }

  .articles-main {
    min-width: 0;
  }

  .article-index-item {
    padding: 1.45rem 0 1.55rem;
    border-bottom: 1px solid #e4dff3;
  }

  .article-index-link {
    display: block;
  }

  .article-index-meta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-bottom: 0.7rem;
    color: var(--text-muted);
    font-size: 0.9rem;
  }

  .article-index-item h2 {
    margin: 0 0 0.7rem;
    font-size: 1rem;
    line-height: 1.16;
    letter-spacing: -0.03em;
    color: #1a1a22;
    font-family: var(--serif);
  }

  .article-index-link:hover h2 {
    color: var(--accent);
  }

  .article-index-excerpt {
    max-width: 54rem;
    margin: 0;
    color: #5d5872;
    font-size: 1rem;
    line-height: 1.88;
  }

  .article-index-footer {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-top: 1rem;
    color: #9a94af;
    font-size: 0.88rem;
  }

  .articles-sidebar {
    position: sticky;
    top: 6rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .sidebar-card {
    padding: 1.15rem 1.2rem;
    border: 1px solid var(--border);
    border-radius: 24px;
    background: transparent;
    box-shadow: none;
  }

  .sidebar-eyebrow {
    margin: 0 0 0.4rem;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.16em;
    font-size: 0.78rem;
    text-decoration: underline;
    text-underline-offset: 0.18em;
  }

  .profile-card h2 {
    margin: 0 0 0.35rem;
    font-size: 1.1rem;
  }

  .profile-card p {
    margin: 0;
    color: #5d5872;
    line-height: 1.7;
    font-size: 0.95rem;
  }

  .sidebar-title {
    margin-bottom: 0.9rem;
    font-size: 0.82rem;
    font-weight: 700;
    color: var(--text-muted);
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .sidebar-title-with-link {
    display: flex;
    justify-content: space-between;
    gap: 0.75rem;
    align-items: baseline;
  }

  .sidebar-title-with-link a {
    color: var(--accent);
    font-size: 0.8rem;
    letter-spacing: normal;
    font-weight: 600;
  }

  .sidebar-list,
  .sidebar-category-list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .sidebar-list li + li,
  .sidebar-category-list li + li {
    margin-top: 0.7rem;
  }

  .sidebar-list a {
    display: block;
    color: #212230;
    line-height: 1.55;
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 0.16em;
  }

  .sidebar-list time {
    display: block;
    margin-top: 0.2rem;
    color: var(--text-muted);
    font-size: 0.82rem;
  }

  .sidebar-category-list a {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    color: #4b4a60;
  }

  .sidebar-category-list a:hover,
  .sidebar-list a:hover {
    color: #4b6fc8;
  }

  .sidebar-tag-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
  }

  .sidebar-tag-groups {
    display: grid;
    gap: 0.95rem;
  }

  .sidebar-tag-section {
    display: grid;
    gap: 0.75rem;
  }

  .sidebar-tag-section-kicker {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.74rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .sidebar-tag-group h3 {
    margin: 0 0 0.55rem;
    font-size: 0.76rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--text-muted);
  }

  .sidebar-tag {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    padding: 0.48rem 0.8rem;
    border-radius: 999px;
    border: 1px solid var(--border);
    color: #54506d;
    background: transparent;
    font-size: 0.84rem;
  }

  .sidebar-tag:hover {
    color: var(--accent);
    border-color: #cdd6f2;
    background: var(--accent-bg);
  }

  .sidebar-tag-free {
    border-style: dashed;
    background: transparent;
  }

  .empty-state {
    text-align: center;
    color: var(--text-muted);
    padding: 3rem;
  }

  @media (max-width: 900px) {
    .series-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .articles-layout {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .articles-sidebar {
      position: static;
    }
  }

  @media (max-width: 640px) {
    .articles-shell {
      padding: 1.5rem 1rem 2rem;
    }

    .series-grid {
      grid-template-columns: 1fr;
    }

    .article-index-item {
      padding: 1rem 0 1.05rem;
    }

    .article-index-item h2 {
      font-size: 1.45rem;
      line-height: 1.16;
    }

    .article-index-excerpt {
      font-size: 0.95rem;
      line-height: 1.75;
    }
  }
</style>
